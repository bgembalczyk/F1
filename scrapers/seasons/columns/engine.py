"""DOMAIN-SPECIFIC: seasons column rule (engine parsing) localized for seasons domain."""

from scrapers.base.table.columns.types.engine_base import BaseEngineColumn


class EngineColumn(BaseEngineColumn):
    """Seasons-specific engine column, inheriting base engine parsing logic."""
