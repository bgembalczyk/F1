[pytest]
testpaths = tests
markers =
    unit: Unit-level tests used by CI marker selection.
    contract: Contract-level tests used by CI marker selection.
    architecture: Architecture-focused tests used by CI marker selection.
