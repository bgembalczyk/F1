[pytest]
testpaths = tests
markers =
    unit: Unit tests for isolated components.
    contract: Contract tests for public interfaces and emitted events.
    architecture: Architecture boundary and layering tests.
